/*
 * 클래스 이름 합치기 — 구현은 `@ssccops/ui`에 있다 (ssccops#243).
 *
 * 세 앱과 form-renderer에 같은 세 줄이 네 벌 있던 것을 걷었다. 여기를 재export로 남기는 것은
 * `@/shared/lib/cn`을 부르는 자리가 많아서다 — 호출부를 건드리지 않고 사본만 없앤다.
 */
export { cn } from "@ssccops/ui";
