/**
 * 가입이 끝난 회원 — 가입 요청의 응답이자, 세션의 `member` 블록과 같은 모양이다.
 *
 * 세션 슬라이스의 `AuthMember`와 필드가 같지만 **가져다 쓰지 않는다.** 같은 레이어의 슬라이스는
 * 서로를 참조하지 않는 것이 이 앱의 규약이고, 무엇보다 두 타입이 답하는 질문이 다르다 —
 * 저쪽은 "지금 이 세션은 누구인가", 이쪽은 "방금 만든 회원은 누구인가"다.
 */
export interface SignedUpMember {
  memberId: number;
  name: string;
}
