/*
 * 테마 고르기 — 구현은 `@ssccops/ui`에 있다 (ssccops#226이 admin에 만들었고 #341에서 올렸다).
 *
 * 상단 바와 모바일 드로어 발치가 **같은 3버튼**을 쓴다(#349). 전에는 상단 바만 아이콘 한
 * 버튼(`ThemeCycleButton`)으로 갈라져 있었는데, 그 컴포넌트는 쓰는 곳이 없어져 지웠다 —
 * 폭이 모자란다는 근거가 실측과 맞지 않았다(자세한 것은 #349). 되살릴 일이 생기면 git
 * 이력에 있다.
 *
 * 자리에 따라 다른 것은 폭을 쓰는 방식뿐이다: 상단 바는 한 줄을 나눠 쓰므로 `fit`,
 * 드로어 발치는 한 줄을 통째로 쓰므로 기본값이다. 둘은 같은 상태를 본다.
 *
 * 여기 shim을 두는 것은 이 앱의 다른 공유 컴포넌트(`badge`·`card`·`notice`·`markdown`)와
 * 같은 모양을 지키기 위해서다 — 화면은 `@/shared/ui`만 보고, 공유 패키지를 직접 부르는 것은
 * `shared/` 안의 이 파일들뿐이다.
 */
export { ThemeToggle } from "@ssccops/ui";
