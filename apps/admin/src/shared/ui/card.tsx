/*
 * 서피스 카드 — 구현은 `@ssccops/ui`에 있다 (ssccops#243).
 *
 * www·lms에 같은 사본이 있었고 이쪽이 자라 있었다(`onClick`·`id`·`CardTitle`·`SectionLabel`).
 * 자란 쪽을 올렸으므로 이 앱의 호출부는 그대로다.
 */
export { Card, CardTitle, SectionLabel } from "@ssccops/ui";
