/*
 * 테마 고르기 — 구현은 `@ssccops/ui`에 있다 (ssccops#226 · 옮긴 것은 ssccops-web#341).
 *
 * lms가 같은 토글을 쓴다. 여기를 재export로 남기는 것은 `shared/ui` 배럴을 통해 부르는
 * 자리를 건드리지 않기 위해서다.
 */
export { ThemeToggle } from "@ssccops/ui";
