/** 더미데이터 기준일 — 실제 시계를 쓰면 D-day 시맨틱이 어긋나므로 고정 */
export const TODAY = "2026-08-09";

/*
 * CSV 이관 매핑 대상 필드(`CSV_FIELDS`)가 있던 자리다 (#57).
 *
 * 목록의 근거가 데이터사전이 아니라 **서버의 `MemberImportField`**여서 entities/member로 옮겼다
 * — 여기 있는 동안에는 서버가 받지 않는 값을 화면이 고르게 둘 수 있었고(반대로 서버가 필드를
 * 늘려도 화면은 몰랐고), 매핑이 필수인 셋(회원명·등급·상태)이라는 사실도 담을 자리가 없었다.
 * 표시 이름도 컬럼ID('회원_명')가 아니라 사람이 읽는 이름('회원명')으로 바뀌었다.
 */

/*
 * 폼 빌더 입력 형식 검증 프리셋 — `@ssccops/form-renderer` 가 정본이다 (#698 · ssccops#516).
 *
 * 그전에는 이 파일이 표를 들고 있었고 `apps/lms` 에 **같은 표가 한 벌 더** 있었다 — 그쪽 주석이
 * «프리셋이 갈리면 어드민에서 고른 학번 형식과 여기서 고른 것이 다른 정규식이 된다»고 스스로
 * 적어 두고 사람이 지키던 자리다. 검증을 한 벌로 두려고 만든 패키지가 바로 옆에 있었는데
 * **정규식만 올라가지 않았다.**
 *
 * 값의 성질은 그대로다 — **고른 순간 문항에 복사된다.** 패키지를 고쳐도 이미 저장된 폼의 검증은
 * 소급되지 않으며, 접수 중인 폼을 바꾸려면 운영진이 그 문항에서 칩을 다시 눌러야 한다.
 */
export { PATTERN_PRESETS } from "@ssccops/form-renderer";
