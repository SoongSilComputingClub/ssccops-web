---
name: commit-message
description: Draft and create a git commit message following this repo's CONTRIBUTING.md convention (issue-numbered "type(scope): 설명" format). Use whenever the user asks to commit changes or write a commit message in this repo.
---

@../../../.agents/skills/commit-message/SKILL.md

이 스킬의 실제 지침은 `.agents/skills/commit-message/SKILL.md`에 있다 (위 `@` 임포트로 이미 로드됐어야 하지만, 만약 내용이 비어 보이면 Read 도구로 해당 파일을 직접 읽어서 그 절차를 그대로 따른다). `.claude`와 `.agents` 양쪽에서 같은 스킬을 쓰는 다른 에이전트 툴들과 내용을 공유하기 위해 실제 내용은 `.agents` 쪽에만 유지한다 — 이 파일에 직접 지침을 추가하지 않는다.
